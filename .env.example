# APRO — Environment Configuration Example

# Application Configuration
APP_ENV=development
APP_HOST=127.0.0.1
APP_PORT=8000
LOG_LEVEL=INFO

# Future Database Configuration
# The database is not implemented in Phase 00.
# DATABASE_URL will be introduced when database infrastructure is implemented.

# Future Razorpay Configuration
# Not required in Phase 00.
# These variables will be introduced when Razorpay integration begins.
